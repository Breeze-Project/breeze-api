name: Setup Java & Gradle
description: Устанавливает JDK 21 и настраивает Gradle

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - uses: gradle/actions/setup-gradle@v4
